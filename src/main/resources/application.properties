server.port=${PORT:8080}

# Database - Supabase PostgreSQL
spring.datasource.url=jdbc:postgresql://db.zduxrxoogtxgtkzivcnf.supabase.co:5432/postgres?sslmode=require&connectTimeout=30&socketTimeout=60
spring.datasource.username=postgres
spring.datasource.password=zNsgkPQLxFqb7W9q
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.initialization-fail-timeout=-1

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT
app.jwt.secret=LandMarketplace2024SecretKeyForJWTTokenGenerationMustBe256BitsLong!
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000,https://land-marketplace.onrender.com,https://land-marketplace-frontend.vercel.app

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
app.upload.dir=/tmp/land-images

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=karthikchowdary0143@gmail.com
spring.mail.password=oknppljsltaepdks
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
app.frontend.url=http://localhost:3000

# Cloudinary
cloudinary.cloud-name=dpdm9m2id
cloudinary.api-key=959514247969871
cloudinary.api-secret=LN07xubWzk89eAFPFR0n-jzxpBQ

# Redis
spring.data.redis.url=${SPRING_DATA_REDIS_URL}
spring.data.redis.ssl.enabled=true

# Jackson
spring.jackson.serialization.write-dates-as-timestamps=false

# Cache
spring.cache.type=redis
spring.cache.redis.time-to-live=60000"spring.jackson.serialization.write-dates-as-timestamps=false" 
